# WebFilter — Список заблокированных сайтов
# Формат: по одному домену на строку
# Поддомены блокируются автоматически (например, vk.com заблокирует также m.vk.com)
# Строки начинающиеся с # — комментарии и игнорируются
# Файл перечитывается автоматически при изменении (без перезапуска прокси)

# ── Социальные сети ──────────────────────────────────────────────────────────
vk.com
ok.ru

# ── Видео / развлечения ──────────────────────────────────────────────────────
rutube.ru

